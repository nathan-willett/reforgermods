enum SCR_EArsenalGameModeType
{
	CAMPAIGN				= 1 << 0, //!< Allows only items with the CAMPAIGN flag in the catalog that have Arsenal Item data to be available in arsenals
	//COMBAT_OPS			= 1 < 1, //!< Allows only items with the COMBAT_OPS flag in the catalog that have Arsenal Item data to be available in arsenals
}
